describe('createContext:isolated', () => {
  it('고립된 Context는 상위 Context를 무시한다.', () => {
  })
  
  it('고립된 Context는 하위에 연결되지 않는다.', () => {
    // TODO 설계 변경 필요
    // 1. Context hierarchy 처리를 위한 React Context 속성과 (PARENT_STORE)
    // 2. Provider를 위한 React Context 속성을 분리한다 (STORE)
  })
})